/* eslint-disable no-unused-vars */
//component = html + css + js
const MyComponent = () => {
    return (
        <div> Trinh Quang Lam</div>

    );
}
export default MyComponent
// Khi chỉ cần export 1 component dùng default. Đặc điểm là
// khi chỉ cần có import từ file này mặc định sẽ là component này => để
// tên thế nào cũng được